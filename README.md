# Banking-Application

A text-based banking application with a user login system. Enables interactions between users - e.g. money transfers, managing contacts. Focuses on managing user data, particularly in tracking changes to one user's data induced by another user. 

This program involved much exception handling, with the goal of handling invalid input without causing the program to terminate. Made use of objects and inheritance to achieve abstraction.
